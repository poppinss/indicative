Ensures the length of a string is not is not less than
the expected length
 
[source, js]
----
const rules = {
  password: 'min:6'
}
 
// or
const rules = {
  password: [
    rule('min', 6)
  ]
}
----